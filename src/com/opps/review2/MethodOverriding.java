package com.opps.review2;

public class MethodOverriding {
	/* Method overloding: It is different implemetations of a method inside the same class
	 * Method overriding : Is is different imlementations of a method in another class
	 * 	1. We need to create a parent and define a method
	 *  2. Create another class and extent it to the firt class (inheritance)
	 *  3. Specify more detailed implematation inside child class
	 *  
	 *  inheritance is a must achieve method overrding
	 *  
	 *  we cannot change return type in method overriding
	 *  
	 *  can we change access modifier while overriding?
	 *  
	 *  can we override static methods? NO? We redeclare if we try to override them..
	 *  
	 *  WebDriver is an interface
	 *  
	 *  FireFoxDriver imlements WebDriver interface
	 *  ChromeDriver implements WebDriver
	 */
}
