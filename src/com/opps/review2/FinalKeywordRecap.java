package com.opps.review2;

public class FinalKeywordRecap {

	// it makes the variable, method or class unchangeable --> final version
	
	/* we can use final keyword with:
	 * - variables --> It makes the variable constant
	 * - method --> Prevent method overriding 
	 * - class --> It prevents inheritance
	 */
}
